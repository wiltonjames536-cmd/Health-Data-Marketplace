[project]
name = 'Health-Data-Marketplace'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.privacy-preserving-compensation]
path = 'contracts/privacy-preserving-compensation.clar'
clarity_version = 3
epoch = 'latest'

[contracts.wearable-data-aggregation]
path = 'contracts/wearable-data-aggregation.clar'
clarity_version = 3
epoch = '3.2'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
